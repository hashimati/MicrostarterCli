package ${pack};

import com.coxautodev.graphql.tools.GraphQLQueryResolver;
import ${domainPackage}.${className};
import ${repoPackage}.${className}Repository;

import javax.inject.Inject;
import javax.inject.Singleton;

@Singleton
public class ${className}QueryResolver implements GraphQLQueryResolver {
    @Inject
    private ${className}Repository ${entityName}Repository;
    public ${className} find(long id)
    {
        return ${entityName}Repository.findById(id).get();
    }
}