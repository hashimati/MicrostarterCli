r2dbc:
  datasources:
    default:
      schema-generate: ${schemaGenerate}
      dialect: POSTGRES
      url: r2dbc:postgresql://localhost:5432/${databaseName}
      username: postgres
      password: ''