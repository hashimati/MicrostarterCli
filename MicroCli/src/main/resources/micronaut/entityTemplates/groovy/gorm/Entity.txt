package ${entitypackage}

import grails.gorm.annotation.Entity

@Entity
class ${className} {
    ${instances}
    static constraints = {
        ${constraints}
    }
    static mapping = {
        collection "${collection}"
        database "${db}"
    }

}