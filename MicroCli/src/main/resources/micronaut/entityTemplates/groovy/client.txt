package ${clientPackage}



import io.micronaut.context.annotation.Parameter
import io.micronaut.http.annotation.*
import io.micronaut.http.client.annotation.Client;
import io.reactivex.Single
${moreImports}
import ${entityPackage}


@Client("/api/${entities}")
interface ${classNameA}Client {

    @Post("/save")
    ${className} save(@Body ${classNameA} ${entityName})

    @Get("/get")
    ${className} findById(@Parameter("id") ${idType} id)

    @Delete("/delete/{id}")
    boolean deleteById(@PathVariable("id") ${idType} id)

    @Get("/findAll")
    Iterable<${className}> findAll()


    @Put("/update")
    ${className} update(@Body ${className} ${entityName})
${methods}
}

