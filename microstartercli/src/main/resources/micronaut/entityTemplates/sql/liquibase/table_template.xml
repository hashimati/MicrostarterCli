            <createTable tableName="${tableName}"
                         remarks="A table to store all ${tableName} records">

               ${columns}
            </createTable>
            <addAutoIncrement columnDataType="bigint"
                              columnName="id"
                              incrementBy="1"
                              startWith="1"
                              tableName="${tableName}"/>